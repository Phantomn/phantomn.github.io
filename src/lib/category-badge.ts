/**
 * Blog post category badges use a fixed sky-blue tint, distinct from the
 * site's brand accent (--primary, amber) - matches the writeups section's
 * per-category color pattern (src/components/writeups/writeup-data-grid.tsx)
 * so badges read as "categorical", not as the primary CTA color.
 */
export const CATEGORY_BADGE_CLASS =
  "border-sky-600/40 bg-sky-500/10 text-sky-700 dark:border-sky-500/40 dark:text-sky-400";
